#include "middle.h"

#include <iostream>

using namespace std;

void itc_num_print(int number) {
    cout << number << endl;
}

int max(int a, int b) {
    return a > b ? a : b;
}

int min(int a, int b) {
    return a < b ? a : b;
}